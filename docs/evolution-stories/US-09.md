# US-09 — Preserve useful tradeoffs between objectives

Issue: [#27](https://github.com/ooples/AiDotNet.Evolution/issues/27). Repository: **AiDotNet.Evolution**.
Integration builds on [US-10 #86](https://github.com/ooples/AiDotNet.Evolution/pull/86), incorporating historical [#52](https://github.com/ooples/AiDotNet.Evolution/pull/52) at `2de0419e8aab7dc656d59af9358134b8483a7544`.

## Status and acceptance

Functionality and local verification complete at `1c2237275449c98df7524ff01dd260a3f8953dfb`. Hosted CI and independent review are separate gates. User handles prerequisite merges and review. No model calls, package publication or merges performed.

As an application developer, I want several valid speed/memory/accuracy tradeoffs so that I can select an implementation matching deployment requirements.

- [x] Given conflicting objectives and hard constraints, when the bounded Pareto archive is used, then only feasible nondominated candidates become deployable winners.
- [x] Given different directions, scales and tolerances, when vectors are compared, then explicit definitions determine dominance; optional infeasible exploration stays separate.
- [x] Given a Pareto run, when snapshotting, querying, selecting, migrating or stopping, then front semantics remain explicit and Best is a chosen representative.
- [x] Given fixed normalization/reference points, when benchmarked at equal call budgets, then hypervolume and individual objectives are compared with scalar-quality search.
- [x] Given scalar consumers, when Pareto is not opted into, then ordering and checkpoint schemas remain intact. Newly explicit unmeasurable-stopping behavior is documented and regression-tested.

## Delivered

Two-to-eight objective axes, explicit bounds/directions/epsilon boxes, bounded crowded-front retention, configurable representatives and deployment queries; exact 2D/3D hypervolume. Separate bounded infeasible exploration never enters winners or default migration. Front-aware selection/topology migration, snapshots, cross-island union, and deterministic checkpoint/restart behavior.

Checkpoint front/pool invariants are checked before task-owned codecs execute. Three-valued stopping distinguishes unmeasurable criteria from stagnation; a configured criterion never measured causes an explicit failure.

Adversarial integration fixes: declared violation-vector lengths reject missing/extra constraints in both pools; schema 9 prevents older readers silently dropping that contract; zero hypervolume improvement thresholds still require positive progress; selection fallback preserves the current island scheduler. US-10's owned-array snapshot optimization and current resource accounting remain intact.

See [public contracts](../PARETO_SEARCH.md).

## Verification and metrics

Release solution build: zero warnings/errors. Tests: 1,165 core net10 + 1,081 net8 + 1,081 net471; 141 program/compiler net10 + 141 net8; 86 profiling = **3,695 .NET executions**, all passed.
TRX: `TestResults/us09-current-stack/`. Python analysis/evidence: **143 passing tests**, including raw-artifact regeneration and corruption rejection. Combined: **3,838 passing test executions**. Final formatting is recorded in the PR handoff.

[Current evidence](../../benchmarks/evidence/pareto/us09-current-stack/README.md): 144 primary runs + 144 complete four-worker replays; all match. Exactly **36,864 charged evaluator calls**, zero model calls.

| Fixture | Scalar-single median HV | Scalar-map64 median HV | Pareto64 median HV | Wins/losses vs map64 |
|---|---:|---:|---:|---:|
| 2 objectives | 0.765323 | 0.923321 | 0.948164 | 24 / 0 |
| 3 objectives | 0.462995 | 0.736014 | 0.751038 | 21 / 3 |

Authored development fixtures only: not measured latency/memory or OpenEvolve superiority. One 3D marginal objective worsens and overhead is higher; all losses remain visible. Pareto remains opt-in.

## Failed approaches and audit

The local index incorrectly labeled historical #52 unimplemented. The initial narrower implementation was rejected as final delivery because it omitted existing exploration/checkpoint/reporting functionality. This integration preserves those contracts.

The first campaign at `09a3a4d` failed before evaluation: its reference genome violated the ownership contract. It now uses an immutable value genome and retains diagnostics. The failed report is preserved and excluded from successful metrics. The intervening prototype evidence remains local under TestResults, not as current product evidence.

Historical #52's inconclusive disconnected-task comparison remains linked from the evidence README. Optional exploration has functional tests, not a measured quality-advantage claim.

## Remaining delivery gates

- [x] Functionality, adversarial review and local .NET verification.
- [x] Retain raw primary/replay evidence and failed campaign.
- [ ] Final hosted CI and independent review on the published dependent PR.
- [ ] User merges prerequisites and this story; issue remains open until merge.
