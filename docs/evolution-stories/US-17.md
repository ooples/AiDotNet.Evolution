# US-17 — Compiler-guided program improvement

Issue: https://github.com/ooples/AiDotNet.Evolution/issues/35

All new implementation is owned by AiDotNet.Evolution. Base: US-08 / PR #51, retaining shared foundation #15.
Historical AiDotNet #2165 is a tracking-only draft, not this implementation and not authorization to keep features there.

As a developer optimizing an algorithm, I want planned edits and bounded repair so that more proposals become
correct, useful programs.

- **Given** an initial program and measured bottleneck, **when** improvement is proposed, **then** a testable hypothesis
  and syntax-addressed edits operate on a detached, fingerprinted multi-file snapshot.
- **Given** compilation or public correctness failure, **when** repair is allowed, **then** bounded diagnostics guide
  a finite repair loop, all dispatched work is charged, and no held-out feedback reaches the planner.
- **Given** a valid patch, **when** it is promoted, **then** compilation, preserved API contract, public correctness and
  held-out performance receipts identify the same source/dependency/compiled-image fingerprint.

Implementation: `src/AiDotNet.Evolution.Programs`, `src/AiDotNet.Evolution.CSharp`.
Acceptance/adversarial tests: `tests/AiDotNet.Evolution.CSharp.Tests`.
Real emitted-image integration: `examples/CompilerGuidedSearch`.
Contracts, trust boundaries and limitations: [compiler guide](../COMPILER_GUIDED_PROGRAMS.md).

## Acceptance evidence

- [x] Multi-file atomic edits, original-snapshot syntax addresses, recorded hypotheses, image/source ownership.
- [x] Compiler then public-correctness repair, finite attempts, missing-receipt/overrun/failure accounting.
- [x] Same-artifact/API/target promotion, nonce/oracle matching, no hidden feedback or hidden-driven retries.
- [x] Compiler-neutral extension API and a runnable authored-fixture process verifier.
- [x] Adversarial review and local verification; no consumer project references.

Production revision: `c4419bf`. The final affected-project rerun passes **30 tests each on net10.0/net8.0**,
both real emitted-image examples and both optional package packs. Core code is unchanged; the final batch passed
**792 net10.0 / 719 net8.0 / 719 net471** core tests, zero failures/skips. Solution build: zero warnings/errors.
The formatting follow-up is whitespace-only; full solution format verification passes. Failed first build (generic
type inference) and first formatting check are retained alongside successful corrections, not discarded.

The new-project coverage run reports **94.26% lines / 74.56% branches** for CSharp and **96.04% / 73.45%** for Programs.
Those are project-specific, not whole-repository rates. [Raw evidence and verification limits](../evidence/us17/README.md).
The authored examples each repair on attempt two and use 27 synthetic work units; their raw failed public result and
all held-out timings remain recorded. They do not establish representative or competitor superiority.

No merge, publication, broad performance claim or automatic issue closure is implied. Current-head hosted checks,
dependency integration/retargeting and independent review remain merge gates. Hostile-code isolation is a required
external verifier boundary, not a built-in guarantee of the authored-fixture example.
