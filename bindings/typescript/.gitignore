dist/
node_modules/
bin/
packages/

# NOT COMMITTED, deliberately. The lockfile records the three per-platform host packages
# as unresolved optional dependencies until they are first published; committing that
# would keep them unresolved for everyone even after they exist on the registry.
package-lock.json
